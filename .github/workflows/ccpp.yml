name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest 
    
    steps:
    - uses: actions/checkout@v2
    - name: make1
      run: g++ Hash_obfs.cpp -o Hash_obfs
    - name: make2
      run: gcc Download_video.c
